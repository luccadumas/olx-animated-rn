export function setLoggedInOperatorToken(token) {
  // localStorage.setItem('operatorToken', token);
}

export function getLoggedInOperatorToken() {
  // const token = localStorage.getItem('operatorToken');

  // if (!token) return null;

  // return token;
}

export function setLoggedInOperator(operator) {
  // localStorage.setItem('operator', JSON.stringify(operator));
}

export function getLoggedInOperator() {
  // const operator = localStorage.getItem('operator');

  // if (!operator) return null;

  // return JSON.parse(operator);
}

export function getLoggedInOperatorRoles() {
  // const operator = getLoggedInOperator();

  // if (!operator || !operator.roles) return null;

  // return operator.roles;
}
